{
  "name": "mcjarfiles-tcadmin-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Cron-triggered Cloudflare Worker that syncs mcjarfiles.com Minecraft server jar versions into TCAdmin 3 Game Updates, so customers get 1-click installs.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.126.0"
  }
}
